# This is an example file showing what your local ".env" file should contain.
# (Note the leading period.)

SECRET_KEY = some-long-random-string

# Site running open-humans-data-processing, our Flask app for data retrieval.
# It should include protocol, domain and port, e.g. "https://127.0.0.1:5000".
DATA_PROCESSING_URL = your-data-processing-url

# Amazon Web Services settings
# We use keys for deployment-specific IAM users to manage permissions.
AWS_ACCESS_KEY_ID = your-aws-access-key-id
AWS_SECRET_ACCESS_KEY = your-aws-secret-access-key
AWS_S3_STORAGE_BUCKET_NAME = your-aws-s3-bucket-name

# 23andme API settings
# Note: in the 23andme API, "App redirect URI" should be set to use the path:
# "/auth/complete/23andme/", e.g. for local development:
# http://127.0.0.1:8000/auth/complete/23andme/
23ANDME_KEY = client-key-here
23ANDME_SECRET = client-secret-here

# The password used for mailgun integration
MAILGUN_PASSWORD=""

# The port the web server should listen on
PORT=
